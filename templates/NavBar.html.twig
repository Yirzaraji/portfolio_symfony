<nav>
  <div class="NavDesktop d-none d-sm-flex">
    <div class="navButton"><a href="{{ path('homepage') }}">HOME</a></div>
    <div class="navButton"><a href="{{ path('cv') }}">CV</a></div>
    <div class="navButton"><a href="#ankor_aboutMe">ABOUT</a></div>
    <div class="navButton"><a href="#ankor_contact">CONTACT</a></div>
    {% if app.user %}
    <div class="navButton"><a class="h5" href="{{ path('admin') }}"><i class="fas fa-users-cog"></i></a></div>
    {% else %}
    <div class="navButton"><a class="h5" href="{{ path('account_login') }}"><i class="fas fa-user-lock"></i></a></div>
    {% endif %}
  </div>
</nav>

<div class="container" id="mobile-burger">
  <div class="row">
    <div class="col-md-12 text-center">
      <button class="ham"></button>
      <nav class="navbar-mobile pt-5">
          <li><a class="menuLink mb-5" href="{{ path('homepage') }}"><i class="fas fa-home"></i> Home</a></li>
          <li><a class="menuLink mb-5" href="{{ path('cv') }}"><i class="fas fa-file-pdf"></i> Cv</a></li>
          <li><a class="menuLink mb-5" href="#ankor_aboutMe"><i class="fas fa-address-card"></i> About</a></li>
          <li><a class="menuLink mb-5" href="#ankor_contact"><i class="fas fa-envelope-open-text"></i> Contact</a></li>
          {% if app.user %}
          <li><a class="menuLink mb-5" href="{{ path('admin') }}"><i class="fas fa-users-cog"></i> Connect</a></li>
          {% else %}
          <li><a class="menuLink mb-5" href="{{ path('account_login') }}"><i class="fas fa-user-lock"></i> Connect</a></li>
          {% endif %}
      </nav>
    </div>
  </div>
</div>